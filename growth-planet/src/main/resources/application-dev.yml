spring:
  datasource:
    url: jdbc:mysql://localhost:3306/growth_planet?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8mb4
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379

wx:
  mock: true
